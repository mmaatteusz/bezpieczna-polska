name: TEMP Dart format probe 2

on:
  pull_request:
    branches: [main]

jobs:
  format-probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.47.4'
          channel: stable
      - name: Print exact Dart formatter diff
        working-directory: mobile
        run: |
          dart format lib/screens/alerts_screen.dart lib/ui/safety_event_card.dart test/start_alert_regression_test.dart
          git diff -- lib/screens/alerts_screen.dart lib/ui/safety_event_card.dart test/start_alert_regression_test.dart
          exit 1
